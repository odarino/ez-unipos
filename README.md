# How to run
Step 1: install server's modules 

    cd ez-unipos
    npm install

Step 2: install client's modules 

    cd /client
    npm install

Step 3:

    npm start

in both 2 folders ( clients & server )
